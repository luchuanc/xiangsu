# RPG-026 全量验收与候选构建

## 元数据

- 状态：DOING
- 关联需求：R-001～R-026
- 关联验收：AC-001～AC-045
- 验收引用：AC-001～AC-045
- 依赖：RPG-023、RPG-024、RPG-025、RPG-027、RPG-028
- 预计文件数：仅验证证据与构建产物；生产代码只允许修复阻塞缺陷

## 目标

执行全量静态、内容、单元、集成、E2E、视觉、离线、存档、十层和 680 场轻量数值回归，生成可玩的自用移动 H5 候选构建和结项证据。

## 非目标

- 不自动部署、发布、上传商店或创建外部账号。
- 不在验收任务中顺手增加玩法、内容或重构架构。

## 前置事实

| 事实 | 文件/symbol | 证据 |
| --- | --- | --- |
| 垂直切片基本可玩性已通过 | RPG-023 | 自动/单人试玩证据 |
| 全 10 层内容已完成 | RPG-024/025 | 内容/E2E 证据 |
| 验收矩阵已冻结 | `verification.md` | V-001～V-045 |

## 精确契约

候选构建必须来自记录的 commit SHA、锁文件和 Node/npm 版本；所有命令原始退出码和产物 hash 写入证据。若发现缺陷，停止验收并创建/回到明确责任任务修复；生产代码修改后必须从受影响门禁重新执行。部署属于新的用户授权动作。

## 文件范围

### 只读检查

- 全部项目生产代码、内容、测试和构建配置
- `docs/requirements/mobile-h5-pixel-rpg/**`
- Git 状态和锁文件

### 允许新增

- `docs/requirements/mobile-h5-pixel-rpg/evidence/release-candidate.md`
- `docs/requirements/mobile-h5-pixel-rpg/evidence/visual/**`
- `dist/**`（由构建命令生成）

### 允许修改

- `verification.md`
- `progress.md`
- `closeout.md`
- 仅用于修复已定位验收阻塞缺陷的最小生产文件，且必须记录原因和回归范围

### 禁止修改

- 已确认需求和验收阈值
- 未经用户授权的部署/托管配置
- 与验收缺陷无关的重构

## 开始前检查

- [x] RPG-023、RPG-024、RPG-025、RPG-027、RPG-028 已完成（RPG-028 的 E2E/balance 环境边界已单独记录）
- [x] `progress.md` 当前任务是本任务
- [ ] `git status --short` 中所有改动来源已确认
- [ ] Node/npm/浏览器版本已记录；真机版本有则记录、无则 NOT_RUN
- [ ] 候选构建输入和内容版本已冻结

## 实施步骤

- 开始前检查：先完成本卡“开始前检查”全部复选项；依赖未 DONE、`progress.md` 未指向本卡或工作区冲突时立即停止。

1. 记录 commit、工作树、Node/npm、锁文件 hash 和 contentVersion。
2. 依次运行 typecheck、lint、内容/本地化校验、final 资源校验、全量单元/集成、全量 E2E 和生产构建。
3. 执行 V-001～V-045，保存指定视口/关键流程截图、冻结内容 canonical JSON、PROFILE-1.2 夹具 canonical JSON、主档 600+替代 80 场逐场/聚合机器可读平衡报告和原始日志。
4. 在 568×320 浏览器视口完成新游戏→十层→深渊回响关键路径抽测；有可用真机时附加复跑 10 分钟，无真机不阻塞。
5. 核对 6 角色、97 技能、18 状态、10 Boss 意图、8 遭遇修正、10 深渊回响、10 层/11 地图/50 遭遇、25 普通/精英、10 Boss、60 装备基础、40+8 装备词条、15 装备触发、20+4 技能词条、18 Combo、5 堆叠物、50 掉落表、7 NPC/对话、1 quest/shop、5 recruitment、348 名称/206 说明与任务描述/7 对话/2 lock/34 标签/25 技能族/12 modifier/9 skill-affix operation/全部核心 UI key/60 错误终端模板，以及 302 内容派生资源/11 tileset/5 系统资源/22 音频。
6. 计算构建文件 hash 与 gzip/bundle 报告，建立候选版本说明。
7. 只在所有门禁 PASS 后填写 closeout；部署保持待用户单独确认。

## 验收与验证

- 验证命令或步骤：严格执行下表全部 V-* 行；命令由前置脚手架建立时，先核对命令真实存在再运行，不得改用未列出的宽范围命令。
- 预期结果：下表每行预期全部满足，自动命令退出码为 0，人工、E2E、快照或报告证据按本卡记录。
- 边界检查：执行下表明确列出的失败、边界、恢复、重复提交、内容负例或最小视口用例；不适用维度以本卡范围为准。

| 验证 ID | 场景 | 命令/步骤 | 预期 |
| --- | --- | --- | --- |
| V-026A | 静态/内容/资源 | 依次运行 `npm run typecheck`、`npm run lint`、`npm run verify:content -- --mode final`、`npm run verify:assets -- --final` | 全部退出码 0；locale token、资源 ID/clip/bundle/source 精确 |
| V-026B | 自动测试 | `npm run test:unit && npm run test:balance -- --profiles lagging,ready,breakthrough,alternative --seeds 20 && npm run test:e2e` | 0 失败、0 意外跳过；夹具匹配 PROFILE-1.2，600+80 场轻量回归及支配性检查通过 |
| V-026C | 候选构建 | `npm run build` | 产物 hash、gzip 和分层 bundle 报告齐全 |
| V-026D | 验收矩阵 | 逐项执行 `verification.md` V-001～V-045 | 全部 PASS；NOT_RUN/FAIL 均阻止结项 |

## 回归范围

全项目、全 10 层、冻结移动视口、离线/后台/恢复、基本可玩性和构建产物。

## 回滚

候选失败时保留证据和最后通过构建，不覆盖部署；回到首个失败门禁对应任务做最小修复。

## 停止条件

- 回滚/no-op理由：前置不满足或契约矛盾时保持 no-op；本卡内验证失败只撤销本卡新增或修改，不回退用户、前置任务或无关文件。

- 任一功能/自动化 V-001～V-045 为 FAIL；仅明确标注“真机建议证据”的 NOT_RUN 不阻塞。
- 工作树含无法解释的用户改动。
- 修复需要改变已确认需求、内容规模或阈值。
- 需要外部部署/托管权限。

## 完成定义

- [ ] 所有自动命令退出码 0
- [ ] V-001～V-045 全部 PASS 且证据可追溯
- [ ] 完整内容数量和十层流程达标
- [ ] 候选构建 hash、环境和基本可玩性记录齐全
- [ ] closeout 已填写，部署仍等待独立授权

## 工程执行协议

- 需求真源：`requirements.md`、`game-design.md`、`ui-spec.md`、`data-contracts.md` 及本任务引用的 1.2 冻结表
- 实施能力：`implementation-coding-agent` 按已确认任务卡实施；不得重新做产品或架构设计
- 排障能力：`superpowers:systematic-debugging`，先复现和定位根因再修复
- 评审能力：`superpowers:requesting-code-review`；评审 Agent 默认只读，修复交回原实现 Agent
- 完成前验证：`superpowers:verification-before-completion`，逐条运行本卡命令并记录真实输出
- Agent：/root/rpg026_implementation
- Agent 类型：LOCAL
- 工作区：`/Users/lcc/temp/xiangsu`；如经批准使用独立 worktree，须先登记绝对路径
- 写入模式：SERIAL
- 写入说明：保留用户和其他任务已有改动，不回退、不覆盖无关文件

## 完成记录

- 完成清单：自动工程/content/assets 门禁与候选构建初审已记录；浏览器、真机、完整主入口和正式平衡仍按停止条件保留为未完成。
- 实际修改文件：`docs/requirements/mobile-h5-pixel-rpg/evidence/release-candidate.md`，以及 `verification.md`、`progress.md` 的真实证据同步；未改生产代码。
- 自动/手工验证结果（最新，2026-08-26）：72 files/372 tests、typecheck/lint/build、fixture/verticalSlice/floors02_05/floors06_10/abyssEchoes/final content/assets PASS；`save-schema` focused 15/15，PartyScreen+GameFlowController focused 2 files/25 tests；获批端口 `playable-flow` 与 `management-flow` 各 1/1 PASS 且无 console/pageerror；`floor-select` 在真实 `obj_f01_n06`/后续战斗中队伍败北，不能记为通过；全量 E2E 本轮未重跑，`playwright --list` 为 18 tests/13 个显式 skip；动态装备/被动倍率与 Combo 派生递归 focused 17/17；balance 仍为 `MISSING_FORMAL_CONTENT_BATCH` 且 results 为空，故本卡严格全量完成定义仍保留 DOING。
- 候选构建输入：package-lock SHA-256 `e36bd3156d864dc13ede782532c200634d83039a7c4f957e0ba7a48f1bdaaf8f`；本轮 `npm run build` 通过并生成 287 个 dist 文件，未将无法确认 convention 的 hash-of-hash 作为验收依据。
- 遗留风险：第 2～10 层、深渊回响完整主入口流程仍缺通过证据；`floor-select` 已暴露真实 starter/resources 的后段战斗资源/难度门槛；正式平衡脚本仍为阻断框架，真机/离线/视觉证据缺失。首层自用功能版可供本地检查，但不宣称严格 V-026A～D 全部完成。
- 用户交付/部署状态：候选构建可供本地自用工程检查；未部署、未托管，需用户另行授权。
