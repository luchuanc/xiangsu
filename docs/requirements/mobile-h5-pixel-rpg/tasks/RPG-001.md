# RPG-001 工程脚手架与质量命令

## 元数据

- 状态：DONE
- 关联需求：R-018
- 关联验收：AC-026、AC-029
- 验收引用：AC-026、AC-029
- 依赖：无
- 预计文件数：脚手架不可分割单元，约 10 个配置/入口文件

## 目标

在空项目目录创建可运行的 Vite + TypeScript strict + PixiJS v8 工程，建立真实的开发、类型、lint、单测、内容校验、E2E 和构建命令，并生成 npm lockfile。

## 非目标

- 不实现游戏场景、业务规则或正式 UI。
- 不初始化或修改父目录 Git；是否在 `xiangsu/` 执行 `git init` 必须先获得用户明确确认。
- 不添加 React、ECS、物理引擎或后端 SDK。

## 前置事实

| 事实 | 文件/symbol | 证据 |
| --- | --- | --- |
| 当前无 package/build 文件 | 项目根 | `context.md` CODE-001/CODE-004 |
| PixiJS v8 初始化是异步 | `Application.init` | PixiJS application skill |
| 当前目录已有 docs，不能用 create-pixi 覆盖 | `docs/` | 当前工作树 |

## 精确契约

`package.json` 必须提供：`dev`、`build`、`typecheck`、`lint`、`test:unit`、`verify:content`、`test:e2e`。生产依赖仅允许 `pixi.js`、`zod`；新增生产依赖必须回到方案门禁。依赖版本以成功安装生成的 `package-lock.json` 为真源，不手写猜测版本。

## 文件范围

### 只读检查

- `docs/requirements/mobile-h5-pixel-rpg/context.md`
- 项目根现有文件

### 允许修改

- 无

### 允许新增

- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `vite.config.ts`
- `eslint.config.js`
- `vitest.config.ts`
- `playwright.config.ts`
- `index.html`
- `src/main.ts`
- `src/styles.css`
- `tests/unit/smoke.test.ts`
- `tests/e2e/smoke.spec.ts`

### 禁止修改

- `docs/requirements/mobile-h5-pixel-rpg/requirements.md`
- `/Users/lcc/temp` 下除 `xiangsu/` 外全部路径

## 开始前检查

- [x] `progress.md` 当前任务是本任务
- [x] 依赖任务全部 DONE（本任务无依赖）
- [x] 已执行 `git status --short --branch`
- [x] 已执行 `node --version` 与 `npm --version`，Node 满足当前 Pixi/Vite 要求
- [x] 用户已决定是否需要独立 Git；当前不初始化独立 Git，只创建工程文件

## 实施步骤

- 开始前检查：先完成本卡“开始前检查”全部复选项；依赖未 DONE、`progress.md` 未指向本卡或工作区冲突时立即停止。

1. 手工创建最小 `package.json`，声明 ESM 和上述脚本，不使用会覆盖 docs 的脚手架命令。
2. 安装 `pixi.js`、`zod`，再安装 TypeScript、Vite、ESLint、Vitest、Playwright 测试依赖，保留 lockfile。
3. 配置 strict TypeScript、DOM/ES 模块和测试路径。
4. 创建 `index.html`、全屏无滚动 CSS 和异步 `bootstrap()` 入口；入口暂只渲染可识别的启动占位。
5. 添加不依赖浏览器的 smoke 单测，并添加 568×320 的 Playwright E2E smoke：打开首页、断言 canvas 与启动占位可见、收集 console/pageerror 并要求为空。
6. 逐条执行命令并修正到全部通过；Playwright 浏览器若需下载且权限不足，记录并请求批准，不静默跳过。

## 验收与验证

- 验证命令或步骤：严格执行下表全部 V-* 行；命令由前置脚手架建立时，先核对命令真实存在再运行，不得改用未列出的宽范围命令。
- 预期结果：下表每行预期全部满足，自动命令退出码为 0，人工、E2E、快照或报告证据按本卡记录。
- 边界检查：执行下表明确列出的失败、边界、恢复、重复提交、内容负例或最小视口用例；不适用维度以本卡范围为准。

| 验证 ID | 场景 | 命令/步骤 | 预期 |
| --- | --- | --- | --- |
| V-001A | 类型 | `npm run typecheck` | 退出码 0 |
| V-001B | lint | `npm run lint` | 退出码 0 |
| V-001C | 单测 | `npm run test:unit` | smoke 通过 |
| V-001D | 构建 | `npm run build` | 生成 `dist/`，无错误 |
| V-001E | 开发页 | `npm run dev -- --host 127.0.0.1` 后打开首页 | 显示启动占位且控制台无错误 |
| V-001F | E2E 烟测 | `npm run test:e2e -- tests/e2e/smoke.spec.ts` | 568×320 首页 canvas/占位可见，console 与 pageerror 均为空 |

## 回归范围

不适用，项目尚无旧运行行为；必须确认 docs 未被脚手架覆盖。

## 回滚

只删除本任务新增的脚手架文件与依赖目录；不得删除 `docs/`。如已创建独立 Git，回滚 Git 元数据需再次征得用户确认。

## 停止条件

- 回滚/no-op理由：前置不满足或契约矛盾时保持 no-op；本卡内验证失败只撤销本卡新增或修改，不回退用户、前置任务或无关文件。

- Node/npm 版本不满足安装要求。
- 安装需要网络但未获批准。
- 当前目录出现未记录的应用文件或用户改动。
- 依赖要求扩大生产技术栈。

## 完成定义

- [x] 七个脚本真实存在并可执行
- [x] 类型、lint、单测和构建通过
- [x] 首页可运行且无控制台错误
- [x] lockfile 已生成
- [x] 未触碰项目目录外文件
- [x] `progress.md` 和 `verification.md` 已更新

## 工程执行协议

- 需求真源：`requirements.md`、`game-design.md`、`ui-spec.md`、`data-contracts.md` 及本任务引用的 1.2 冻结表
- 实施能力：`implementation-coding-agent` 按已确认任务卡实施；不得重新做产品或架构设计
- 排障能力：`superpowers:systematic-debugging`，先复现和定位根因再修复
- 评审能力：`superpowers:requesting-code-review`；评审 Agent 默认只读，修复交回原实现 Agent
- 完成前验证：`superpowers:verification-before-completion`，逐条运行本卡命令并记录真实输出
- Agent：/root/rpg001_implementation
- Agent 类型：LOCAL
- 工作区：`/Users/lcc/temp/xiangsu`；如经批准使用独立 worktree，须先登记绝对路径
- 写入模式：SERIAL
- 写入说明：保留用户和其他任务已有改动，不回退、不覆盖无关文件

## 完成记录

- 完成清单：完成定义全部勾选；实际文件、命令输出、未执行项、Agent/工作区和剩余风险均已填写。
- 实际修改文件：新增 `package.json`、`package-lock.json`、`tsconfig.json`、`vite.config.ts`、`eslint.config.js`、`vitest.config.ts`、`playwright.config.ts`、`index.html`、`src/main.ts`、`src/styles.css`、`tests/unit/smoke.test.ts`、`tests/e2e/smoke.spec.ts`。
- 实际依赖版本：生产依赖 `pixi.js@8.19.0`、`zod@4.4.3`；开发依赖 Vite 6.4.3、TypeScript 6.0.3、ESLint 9.39.5、Vitest 3.2.7、Playwright 1.61.1；Node 18.20.8、npm 10.8.2。
- 实际命令与结果：主 Agent 复跑 `typecheck`、`lint`、`verify:content -- --mode fixture`、`build` 均退出码 0；`test:unit` 2/2 通过；568×320 E2E 1/1 通过且 console/pageerror 为空。实现 Agent 先获得 `startApplication is not a function` 的 RED，再实现失败收口并转 GREEN；只读复审 APPROVED，P0/P1/P2=0/0/0。
- 未执行检查：未单独长期运行 dev server 做人工浏览；同一页面成功路径已由 Playwright 启动 Vite 并覆盖。
- 与计划差异：首轮评审后新增 `startApplication()` 收口 Pixi 初始化失败并显示“启动失败，请刷新重试”；未初始化或修改 Git。
- 剩余风险：重复调用启动入口的幂等、销毁与失败后重试归 RPG-005/006 生命周期实现，不阻塞本卡。
- 下一任务：RPG-003；其完成后解锁 RPG-002。
