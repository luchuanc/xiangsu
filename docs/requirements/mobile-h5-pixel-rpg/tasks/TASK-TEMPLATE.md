# RPG-NNN 任务标题

## 元数据

- 状态：TODO
- 关联需求：R-NNN
- 关联验收：AC-NNN
- 验收引用：填写本卡实际 AC-NNN 集合
- 依赖：无
- 预计文件数：按最小可审查单元填写

## 目标

用一句话描述本卡完成后的可验证结果。

## 非目标

- 明确本卡不做的相邻工作。

## 前置事实

| 事实 | 文件/symbol | 证据 |
| --- | --- | --- |
| 已冻结契约 | 目标 symbol | 真源章节或前置任务证据 |

## 精确契约

逐字段引用需求真源；不得在任务卡创造未确认字段、别名、默认值或兼容分支。

## 文件范围

### 只读检查

- 直接依赖文件

### 允许新增

- 新文件绝对/项目相对路径

### 允许修改

- 已有文件绝对/项目相对路径

### 禁止修改

- 无关模块、冻结需求和用户既有改动

## 开始前检查

- [ ] 所有依赖任务为 DONE
- [ ] `progress.md` 当前任务是本任务
- [ ] 工作区改动和文件所有权已登记

## 实施步骤

- 开始前检查：确认依赖 DONE、`progress.md` 指向本卡、目标文件存在且工作区无所有权冲突。

1. 先写失败测试或可复现基线。
2. 实施最小范围改动。
3. 运行目标验证与回归。

## 验收与验证

- 验证命令或步骤：填写下表全部真实命令或人工步骤。
- 预期结果：填写退出码、快照、E2E 或人工检查的二值结果。
- 边界检查：填写负向、边界、恢复、幂等或最小视口检查。

| 验证 ID | 场景 | 命令/步骤 | 预期 |
| --- | --- | --- | --- |
| V-NNNA | 目标行为 | 真实命令/步骤 | 可观察通过条件 |

## 回归范围

列出直接和高风险相邻模块。

## 回滚

说明可恢复动作，不使用破坏性宽范围命令。

## 停止条件

- 回滚/no-op理由：前置不满足时保持 no-op；失败时只撤销本卡范围，不回退用户或前置任务改动。

- 契约矛盾、缺字段或需要扩大产品/架构范围。

## 完成定义

- [ ] 验收全部通过
- [ ] 评审和真实验证证据已记录

## 工程执行协议

- 需求真源：填写 REQ/GAME/UI/DATA/冻结表具体章节
- 实施能力：`implementation-coding-agent` 按已确认任务卡实施
- 排障能力：`superpowers:systematic-debugging`
- 评审能力：`superpowers:requesting-code-review`
- 完成前验证：`superpowers:verification-before-completion`
- Agent：一个写入型 Agent 唯一持有本卡
- Agent 类型：
- 工作区：填写绝对路径或已登记 worktree
- 写入模式：SERIAL
- 状态真源写入：实现 Agent 不修改本任务卡、`progress.md` 或 `verification.md`；主 Agent 复核后唯一回写。

## 完成记录

- 完成清单：完成定义全部勾选，并填写实际文件、验证输出、Agent/工作区和剩余风险。
- 实际修改文件：
- 验证结果：
- 与计划差异：
- 剩余风险：
- 下一任务：
