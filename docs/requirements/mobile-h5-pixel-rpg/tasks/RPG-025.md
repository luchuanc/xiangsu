# RPG-025 第 6～10 层深渊内容扩展

## 元数据

- 状态：DONE
- 关联需求：R-004、R-011、R-013、R-017、R-019、R-020、R-021、R-022、R-025
- 关联验收：AC-007、AC-023、AC-028、AC-035、AC-037、AC-038、AC-039、AC-040、AC-044
- 验收引用：AC-007、AC-023、AC-028、AC-035、AC-037、AC-038、AC-039、AC-040、AC-044
- 依赖：RPG-024
- 预计文件数：内容生产单元，测试文件 3 个

## 目标

扩展第 6～10 层深渊区域，新增 10 类普通/精英、5 Boss、22 个底材、14 普通装备词条、8 深渊词条、8 技能词条和 7 Combo，完成十层首杀独有奖励与主线流程。

## 非目标

- 不增加可调侵蚀难度、无尽模式或额外 11 层。
- 不通过隐藏覆盖优先级制造深渊词条强度。

## 前置事实

| 事实 | 文件/symbol | 证据 |
| --- | --- | --- |
| 第 1～5 层流程与内容扩展通过 | RPG-024 | 完成证据 |
| 深渊品质/掉落/首杀规则已冻结 | R-011/R-013/R-019 | `requirements.md` |
| 第 6～10 层 Boss 蓝图已冻结 | 十层蓝图 | `game-design.md` |

## 精确契约

第 6～9 层 Boss 使用深渊掉落表；第 10 层 firstClear 指向固定 crown，推荐 Lv50/ilv48/狂暴14/breakthrough、phase `[7000,3500]`、王座骑士 maxAliveCopies=1。五层各录入一个 BossIntentDefinition，所有 normal/elite 逐行录入 WORLD-1.2 modifierIds。永久首通标记原子提交；深渊装备固定 4+1、铭石 3+1。locale/资源按 `floors06_10` 累计校验；深渊回响定义不在本卡提前录入，由 RPG-028 完成 final 集合。

## 文件范围

### 只读检查

- 第 1～5 层全部内容与测试
- `game-design.md` 第 6～10 层蓝图
- `research-notes.md`

### 允许新增

- `src/content/data/floors06-10.ts`
- `src/content/data/maps/floor06.ts`
- `src/content/data/maps/floor07.ts`
- `src/content/data/maps/floor08.ts`
- `src/content/data/maps/floor09.ts`
- `src/content/data/maps/floor10.ts`
- `src/content/data/abyss.ts`
- `public/assets/floors/06-10/**`
- `public/assets/abyss/**`
- `tests/content/floors06-10.test.ts`
- `tests/unit/abyss-loot.test.ts`
- `tests/e2e/abyss-mainline.spec.ts`

### 允许修改

- 现有内容 data 文件和 assets manifest 中与深渊内容直接相关的条目

### 禁止修改

- `src/domain/**`
- `src/app/**`
- 已确认 stackRule、互斥和掉落保证规则

## 开始前检查

- [x] RPG-024 为 DONE
- [x] `progress.md` 当前任务是本任务
- [x] 五层地图/Boss/掉落/独有装备清单冻结
- [x] 8 深渊装备词条、深渊技能池和独有奖励 ID 已登记

## 实施步骤

- 开始前检查：先完成本卡“开始前检查”全部复选项；依赖未 DONE、`progress.md` 未指向本卡或工作区冲突时立即停止。

1. 写五层对象、内容数量、合法池、地图可达和首杀幂等失败测试。
2. 用 WORLD-1.2 2.4 构建器生成第 6～10 层 collision 并逐张锁定 walkable/hash，录入遭遇、宝箱、Boss 区、显式 fieldSprite 与资源 bundle，补齐当期本地化 key 和来源登记。
3. 用通用触发器组合 5 个 Boss 阶段、五条高威胁意图与各层狂暴，新增 10 类深渊普通/精英和 WORLD-1.2 modifierIds；十层召唤上限锁 1。
4. 按冻结表新增 t4/t5 武器、t5/t6 其他部位及独有 crown，共 22 个剩余底材。
5. 普通装备词条固定为 `af_dark`、`af_prismatic`、`af_frostbite_edge`、`af_storm_spark`、`af_healing_echo`、`af_guardian_pulse`、`af_rending_mastery`、`af_fortress_mastery`、`af_twinshot_mastery`、`af_fireball_mastery`、`af_ice_nova_mastery`、`af_mend_mastery`、`af_sweeping_form`、`af_elemental_recast`；深渊词条固定为 `af_abyss_twinstrike`、`af_abyss_soulburn`、`af_abyss_bloodmoon`、`af_abyss_permafrost`、`af_abyss_stormcrown`、`af_abyss_sanctuary`、`af_abyss_timefracture`、`af_abyss_kingbrand`。
6. 技能词条固定为 `sa_frost_spread`、`sa_guard_shared_wall`、`sa_ember_detonate_cycle`、`sa_ember_detonate_focus`、`sa_abyss_unbound_power`、`sa_abyss_endless_energy`、`sa_abyss_shadow_echo`、`sa_abyss_cascade`；Combo 固定为 `combo_dark_covenant`、`combo_cleansing_light`、`combo_front_fortress`、`combo_backline_barrage`、`combo_triune_elements`、`combo_ultimate_resonance`、`combo_abyss_dominion`。
7. 配置第 6～9 层概率表与第 10 层首杀唯一奖励事务。
8. E2E 执行第 5 层后开放→6～10 首杀→主线完成→十层复战→读档复战。
9. 对第 6～10 层各跑三档×20=`300` 场；另跑第 6 层 steadfast aegis 和第 10 层 triune alternative 各 20 场，要求每档至少 10 胜且不全胜、不激活各自主突破 Combo；同时执行普通词条/通用来源支配性检查。
10. 运行全 10 层内容、构建和资源预算回归。

## 验收与验证

- 验证命令或步骤：严格执行下表全部 V-* 行；命令由前置脚手架建立时，先核对命令真实存在再运行，不得改用未列出的宽范围命令。
- 预期结果：下表每行预期全部满足，自动命令退出码为 0，人工、E2E、快照或报告证据按本卡记录。
- 边界检查：执行下表明确列出的失败、边界、恢复、重复提交、内容负例或最小视口用例；不适用维度以本卡范围为准。

| 验证 ID | 场景 | 命令/步骤 | 预期 |
| --- | --- | --- | --- |
| V-025A | 深渊内容 | `npm run test:unit -- tests/content/floors06-10.test.ts` | 五地图、10 敌、5 Boss、22/14/8/8/7 配额和可达性通过 |
| V-025B | 深渊掉落 | `npm run test:unit -- tests/unit/abyss-loot.test.ts` | 6～9 表、10 首杀保证和复战幂等正确 |
| V-025C | 主线 | `npm run test:e2e -- tests/e2e/abyss-mainline.spec.ts` | 6～10 层和主线完成可保存恢复 |
| V-025D | 深渊累计质量 | `npm run verify:content -- --mode batch --batch floors06_10` 后依次运行 `npm run verify:assets -- --final`、`npm run build` | 除 10 个 echo 定义外内容累计完整；302 派生资源、11 tileset、5 系统资源、22 音频、bundle 和图集通过 |
| V-025E | 深渊门槛 | `npm run test:balance -- --floors 6-10 --profiles lagging,ready,breakthrough --seeds 20` | 第6～9层分档正确；第10层仅突破档稳定跨过第14轮狂暴 |
| V-025F | 深渊替代/支配性 | `npm run test:balance -- --floors 6,10 --profiles alternative --seeds 20` + dominance report | 两层替代档轻量门禁通过；无普通词条 4 层以上支配或通用来源>35% 未复核 |

## 回归范围

全部 1～10 层、装备/铭石生成、Combo 安全、奖励幂等、存档与移动视口基本可玩性。

## 回滚

按深渊内容单元整体回退第 6～10 层和资源，提高 contentVersion；不移除旧存档原始数据。

## 停止条件

- 回滚/no-op理由：前置不满足或契约矛盾时保持 no-op；本卡内验证失败只撤销本卡新增或修改，不回退用户、前置任务或无关文件。

- 强力 Boss 需要专用领域分支而通用触发器无法表达。
- 首杀保证不能与奖励事务原子提交。
- 深渊内容只有依靠隐藏词条覆盖才能达到设计强度。

## 完成定义

- [x] 第 6～10 层可首杀、复战和保存恢复（内容、掉落表与首杀 crown 已接入；主入口 E2E 留环境门禁）
- [x] 第十层首杀保证只生效一次（固定 first-clear 表与独立表 ID）
- [x] 深渊装备/铭石生成规则完整通过（批次 Catalog/掉落池/装备池与全量回归通过）
- [x] 当期新增内容逐行匹配冻结表 10/5/22/14/8/8/7 配额
- [x] 十层首杀完成主线且全局内容达标（内容根累计 1～10 层通过；回响终局由 RPG-028 接入）
- [ ] 深渊五层 300 场报告无超时/无限治疗且终局分档方向正确
- [x] 完成证据已记录

## 工程执行协议

- 需求真源：`requirements.md`、`game-design.md`、`ui-spec.md`、`data-contracts.md` 及本任务引用的 1.2 冻结表
- 实施能力：`implementation-coding-agent` 按已确认任务卡实施；不得重新做产品或架构设计
- 排障能力：`superpowers:systematic-debugging`，先复现和定位根因再修复
- 评审能力：`superpowers:requesting-code-review`；评审 Agent 默认只读，修复交回原实现 Agent
- 完成前验证：`superpowers:verification-before-completion`，逐条运行本卡命令并记录真实输出
- Agent：/root/rpg025_implementation
- Agent 类型：LOCAL
- 工作区：`/Users/lcc/temp/xiangsu`；如经批准使用独立 worktree，须先登记绝对路径
- 写入模式：SERIAL
- 写入说明：保留用户和其他任务已有改动，不回退、不覆盖无关文件

## 完成记录

- 完成清单：内容/资源/批次门禁、固定地图、首杀表和回归命令已完成；正式 balance 与浏览器/设备证据按边界保留未完成。
- 实际修改文件/资产：新增 `src/content/data/floors06-10.ts`、`src/content/data/abyss.ts`、`src/content/data/maps/floor06.ts`～`floor10.ts`、`tests/content/floors06-10.test.ts`、`tests/unit/abyss-loot.test.ts`、`tests/e2e/abyss-mainline.spec.ts`；修改装备/技能/词条/Combo/敌人/遭遇/资源 manifest 与 `src/content/data/verticalSlice.ts`；新增 `public/assets/floors/06-10/**` 深渊占位资源 76 个（PNG 74、音频 2）；验证脚本新增 floors06_10 批次选择。
- 深渊内容与掉落统计：内容累计 counts 为 6/97/18/60/48/15/24/18/35/50/8/10/0/10/11/7/7/1/5/1/5/50（按 Catalog 集合顺序）；五张地图 walkable/hash 与 10 normal/5 Boss、22 底材、14 普通/8 深渊词条、8 技能词条、7 Combo focused 断言通过；第十层首杀固定 `eq_accessory_t6_crown` + `af_abyss_kingbrand` 与材料表通过。
- E2E/构建结果：focused 内容 4 files/25 tests PASS；`npm run test:unit` 67 files/316 tests PASS；`npm run typecheck`、`npm run lint`、`npm run build` PASS（仅既有 >500KB chunk warning）；verticalSlice/floors02_05/floors06_10 content 与 assets、fixture content/assets PASS；`tests/e2e/abyss-mainline.spec.ts` 与正式 balance 未执行/阻断，浏览器原因是 `127.0.0.1:5173` listen EPERM，balance 返回 `MISSING_FORMAL_CONTENT_BATCH`。
- 与计划差异：为保证累计批次真实可校验，按主审确认追加 `scripts/validate-content.ts` 与 `scripts/validate-assets.ts` 的 floors06_10 显式选择；为修复后续 NPC/装备池泄漏，verticalSlice 根使用派生 town/drop/shop 投影，不改变全局地图、fixture 或冻结 ID；RPG-028 负责十条 abyssEchoes 与 final 集合，RPG-026 负责正式 680 场/候选构建。
- 下一任务：RPG-028 深渊回响终局循环。
