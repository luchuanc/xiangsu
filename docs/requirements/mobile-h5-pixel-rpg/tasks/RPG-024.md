# RPG-024 第 2～5 层内容扩展

## 元数据

- 状态：DONE
- 关联需求：R-004、R-017、R-018、R-020、R-021、R-022、R-025
- 关联验收：AC-007、AC-028、AC-035、AC-037、AC-039、AC-040、AC-044
- 验收引用：AC-007、AC-028、AC-035、AC-037、AC-039、AC-040、AC-044
- 依赖：RPG-022、RPG-023
- 预计文件数：内容生产单元，测试文件 2 个

## 目标

沿用冻结表扩展第 2～5 层普通区域，新增 10 类普通/精英、4 Boss、27 个底材、14 普通装备词条、8 技能词条和 7 Combo，并让 6 名角色均可获得，不新增运行时特例。

## 非目标

- 不制作第 6～10 层深渊内容。
- 不改变第一层已验证核心循环、领域公式或数据 schema。

## 前置事实

| 事实 | 文件/symbol | 证据 |
| --- | --- | --- |
| 垂直切片与移动质量门禁通过 | RPG-022/023 | 完成证据 |
| 第 2～5 层主题/机制已冻结 | 十层蓝图 | `game-design.md` |
| 内容必须配置驱动 | R-017 | `requirements.md` |

## 精确契约

每层必须有唯一 MapDefinition、含推荐/阶段/短程字段的 FloorDefinition、带显式 fieldSpriteId/modifierIds 的 EncounterDefinition、一个 BossIntentDefinition 和掉落表。Boss 阶段/意图/狂暴只组合通用定义。locale 与资源按 `floors02_05` 累计批次精确校验；禁止修改 `src/domain/**`。

## 文件范围

### 只读检查

- 第 1 层全部内容与测试
- `game-design.md` 第 2～5 层蓝图
- `src/content/contracts.ts`

### 允许新增

- `src/content/data/floors02-05.ts`
- `src/content/data/maps/floor02.ts`
- `src/content/data/maps/floor03.ts`
- `src/content/data/maps/floor04.ts`
- `src/content/data/maps/floor05.ts`
- `public/assets/floors/02-05/**`
- `tests/content/floors02-05.test.ts`
- `tests/e2e/floors02-05.spec.ts`

### 允许修改

- `src/content/data/maps/town.ts`
- `src/content/data/npcs.ts`
- `src/content/data/dialogues.ts`
- `src/content/data/characters.ts`
- `src/content/data/recruitment.ts`
- `src/content/data/quests.ts`
- `src/content/data/enemies.ts`
- `src/content/data/encounters.ts`
- `src/content/data/equipment.ts`
- `src/content/data/skills.ts`
- `src/content/data/skillAffixes.ts`
- `src/content/data/combos.ts`
- `src/content/data/assets.manifest.ts`
- `src/content/locales/zh-CN.ts`

### 本轮经确认追加的最小接线范围

- `src/content/Catalog.ts`：仅修正 `floors02_05` 累计 manifest 的 t4 非武器底材与 16 条技能词条集合。
- `src/content/data/floors02-05.ts`：仅按本批 manifest 过滤掉落/商店装备池，避免未来批次底材泄漏。
- `scripts/validate-content.ts`、`scripts/validate-assets.ts`：仅为 `floors02_05` 选择对应累计根/资源 manifest；fixture、final 和其它批次语义不变。

### 禁止修改

- `src/domain/**`
- `src/app/**`
- 第 1 层规则数据以掩盖回归

## 开始前检查

- [x] RPG-022、RPG-023 为 DONE（移动浏览器三循环证据保留 NOT_RUN_ENV，不伪造）
- [x] `progress.md` 当前任务是本任务
- [x] 四层内容清单、地图草图、Boss 通用机制拆解均完成
- [x] 新资产来源、尺寸和 bundle 预算已登记

## 实施步骤

- 开始前检查：先完成本卡“开始前检查”全部复选项；依赖未 DONE、`progress.md` 未指向本卡或工作区冲突时立即停止。

1. 先写四层必需对象、引用、地图可达、Boss 和掉落表的失败测试。
2. 用 WORLD-1.2 2.4 构建器生成第 2～5 层 collision 并逐张锁定 walkable/hash，录入出生点、遭遇、宝箱、层主区域和安全位置；在城镇补地图学家与深渊守望者，使七类 NPC 全部落位，守望者按第五层首通解锁。
3. 新增 10 类普通/精英、4 个唯一 Boss、四组 Boss 意图、WORLD-1.2 对应 modifierIds、通用 AI 与 Boss 阶段组合，并配置两条可达招募。
4. 按冻结表新增 t2 其他部位、t2 武器+t3 其他部位、t3 武器+t4 其他部位，共 27 个底材。
5. 装备词条固定为 `af_ferocity`、`af_focus`、`af_resolve`、`af_physical_edge`、`af_lightning`、`af_holy`、`af_poison`、`af_high_spirit`、`af_executioner`、`af_last_stand`、`af_frontline_wall`、`af_backline_focus`、`af_marked_prey`、`af_venom_edge`。
6. 技能词条固定为 `sa_frost_nova_focus`、`sa_frost_zero_echo`、`sa_ranger_twin_echo`、`sa_ranger_marked_chase`、`sa_swift_charge`、`sa_storm_chain`、`sa_universal_focus`、`sa_priest_sanctuary_echo`；Combo 固定为 `combo_venom_bloom`、`combo_frozen_verdict`、`combo_storm_circuit`、`combo_execution_cadence`、`combo_steadfast_aegis`、`combo_swift_formation`、`combo_perfect_strike`。
7. 登记/打包每层 fieldSprite、battleSprite、tileset、技能动画与音频依赖，补齐当期本地化 key，逐层验证 bundle/图集/来源门禁。
8. E2E 依次首杀解锁 2→3→4→5→6，并验证重复挑战、远征刷新、每轮首枚精英铭石专注和卡点诊断。
9. 对第 2～5 层各跑三档×20=`240` 场；另跑第 3 层 `alternative(combo_perfect_strike)×20`，断言至少 10 胜且不全胜、不激活主 storm circuit；保存逐场/聚合 JSON。
10. 运行第一层回归，确保内容扩展不改变既有种子快照。

## 验收与验证

- 验证命令或步骤：严格执行下表全部 V-* 行；命令由前置脚手架建立时，先核对命令真实存在再运行，不得改用未列出的宽范围命令。
- 预期结果：下表每行预期全部满足，自动命令退出码为 0，人工、E2E、快照或报告证据按本卡记录。
- 边界检查：执行下表明确列出的失败、边界、恢复、重复提交、内容负例或最小视口用例；不适用维度以本卡范围为准。

| 验证 ID | 场景 | 命令/步骤 | 预期 |
| --- | --- | --- | --- |
| V-024A | 内容 | `npm run test:unit -- tests/content/floors02-05.test.ts` | 四地图、10 敌、4 Boss、27/14/8/7 配额、掉落和可达性通过 |
| V-024B | 层流程 | `npm run test:e2e -- tests/e2e/floors02-05.spec.ts` | 依次解锁、复战和远征刷新正确 |
| V-024C | 累计校验 | `npm run verify:content -- --mode batch --batch floors02_05` | 0 错误且本批累计所有合法池非空 |
| V-024D | 构建/预算 | `npm run build` 并检查 bundle 报告 | 每层 bundle/图集不超 R-018 |
| V-024E | 普通区门槛 | `npm run test:balance -- --floors 2-5 --profiles lagging,ready,breakthrough --seeds 20` | 四层轻量回归的狂暴/轮数达到 AC-037/039 |
| V-024G | 第三层替代构筑 | `npm run test:balance -- --floors 3 --profiles alternative --seeds 20` | 20 场至少 10 胜且不全胜、主 Combo 未激活 |
| V-024F | locale/资源批次 | `npm run verify:content -- --mode batch --batch floors02_05` 后运行 `npm run verify:assets -- --batch floors02_05` | 累计第 1～5 层 key/资源精确，无缺图、额外 ID 或错误 owner bundle |

## 回归范围

第一层全流程、内容校验、掉落确定性、存档解锁和移动视口基本可玩性。

## 回滚

按第 2～5 层内容单元整体回退新增数据和资源；提升 contentVersion，不改领域代码或旧存档字段。

## 停止条件

- 回滚/no-op理由：前置不满足或契约矛盾时保持 no-op；本卡内验证失败只撤销本卡新增或修改，不回退用户、前置任务或无关文件。

- 某 Boss 必须新增领域硬编码才能工作。
- 任一层 bundle 超限且无法通过资源复用/压缩解决。
- 内容扩展改变已有种子 golden 结果。

## 完成定义

- [x] 第 2～5 层内容、地图对象、首杀奖励与复战数据可由累计根严格构造
- [x] 城镇七类 NPC 全部落位且深渊守望者锁定/解锁字段已录入
- [x] 6 名角色在第五层前均有可达招募来源
- [x] 当期新增内容逐行匹配冻结表 10/4/27/14/8/7 配额
- [x] 第五层首杀奖励与第六层门禁字段已录入
- [x] 所有 Boss 使用通用机制组合
- [ ] 第 2～5 层落后会卡、整备能过、突破明显缩时的正式模拟证据（模拟器当前返回 `MISSING_FORMAL_CONTENT_BATCH`，转由 RPG-026）
- [x] 内容、资源预算和第一层回归通过
- [x] 完成证据已记录；浏览器 E2E 按环境边界记录 NOT_RUN_ENV

## 工程执行协议

- 需求真源：`requirements.md`、`game-design.md`、`ui-spec.md`、`data-contracts.md` 及本任务引用的 1.2 冻结表
- 实施能力：`implementation-coding-agent` 按已确认任务卡实施；不得重新做产品或架构设计
- 排障能力：`superpowers:systematic-debugging`，先复现和定位根因再修复
- 评审能力：`superpowers:requesting-code-review`；评审 Agent 默认只读，修复交回原实现 Agent
- 完成前验证：`superpowers:verification-before-completion`，逐条运行本卡命令并记录真实输出
- Agent：主 Agent 接管（/root/rpg024_implementation_v2 中断后）+ /root/rpg024_manifest_fix + /root/rpg024_cli_wiring
- Agent 类型：LOCAL
- 工作区：`/Users/lcc/temp/xiangsu`；如经批准使用独立 worktree，须先登记绝对路径
- 写入模式：SERIAL
- 写入说明：保留用户和其他任务已有改动，不回退、不覆盖无关文件

## 完成记录

- 完成清单：四层内容、地图、敌人/Boss、NPC/招募、装备/技能词条、Combo、资源和严格累计校验已完成；正式 balance 与浏览器流程按证据边界保留未完成项。
- 实际修改文件/资产：`src/content/data/{floors02-05.ts,equipment.ts,assets.manifest.ts,skills.ts,enemies.ts,encounters.ts,combos.ts}`、`src/content/data/maps/{town,floor02,floor03,floor04,floor05}.ts`、`src/content/Catalog.ts`、`scripts/validate-content.ts`、`scripts/validate-assets.ts`、`tests/content/floors02-05.test.ts`、`tests/e2e/floors02-05.spec.ts`、`public/assets/floors/02-05/**`，以及本卡既有 locale/NPC/recruitment 内容文件。
- 分层内容与 bundle 统计：累计 batch content counts 为 characters=6、skills=85、statuses=18、equipmentBases=38、equipmentAffixes=26、affixTriggers=8、skillAffixes=16、combos=11、enemies=20、encounters=25、encounterModifiers=8、bossIntents=5、floors=5、maps=6、npcs=7、dialogues=7、quests=1、recruitments=5、shops=1、items=5、dropTables=25；floors02_05 资源 168 项，78 个本层物理占位资源。
- E2E/回归结果：`npx vitest run --config /dev/null tests/content/floors02-05.test.ts --environment node` 11/11；`npm run test:unit` 66 files/314 tests；typecheck/lint/build、content/assets batch+fixture 全部 PASS；E2E 因 `127.0.0.1:5173` listen EPERM 未执行；balance 两组命令退出 0 但返回 `blocked=true reason=MISSING_FORMAL_CONTENT_BATCH`，未伪造正式结果。
- 与计划差异：按用户继续指示追加了累计 manifest/批次池和两条验证脚本的最小接线；保留全局 t4 武器定义供后续深渊批次，floors02_05 只暴露冻结的 t4 防具/饰品；未修改 domain、Catalog 校验逻辑、index、Vitest 配置或运行时。
- 下一任务：RPG-025 第 6～10 层深渊内容扩展；正式 balance 由 RPG-026 在全批次模拟器接入后执行。
